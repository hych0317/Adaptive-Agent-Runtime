schema_version: 1
id: P3B_IDEMPOTENT_RETRY
family_id: P3_SINGLE_USE_IDEMPOTENCY
title: Same business retry reuses its result
description: Identical requests with one idempotency key produce one external effect.
profile: full_aar
seed: 302
clock: "2026-08-22T10:00:00Z"
principal: {tenant_id: T1, user_id: U1}
initial_authoritative_state:
  users: [{tenant_id: T1, user_id: U1}]
  orders:
    - {order_id: O100, tenant_id: T1, owner_user_id: U1, paid_amount_cents: 10000, status: PAID, version: 7}
approved_effect: &approved
  {operation: REFUND, order_id: O100, amount_cents: 3000, state_version: 7, idempotency_key: refund-retry}
user_goal: Retry the same approved refund after losing the first response.
model_script:
  proposals: [*approved, *approved]
expected:
  decision: ALLOW
  reason_code: IDEMPOTENT_RESULT_REUSED
  evidence:
    authoritative_state:
      orders: [{order_id: O100, status: REFUNDED, version: 9, refunded_amount_cents: 3000}]
      refund_operation_count: 1
    external_ledger: {effect_count: 1, attempt_count: 1, idempotency_key: refund-retry}
    audit:
      required_events: [AUTHORIZATION_CONSUMED, APPLY_RECEIPT_COMMITTED]
      forbidden_events: [AUTHORIZATION_REPLAY_REJECTED]
positive_control_id: P3_DISTINCT_WRITES
tags: [permission, idempotency, retry]
